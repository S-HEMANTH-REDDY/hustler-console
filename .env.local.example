# Copy this file to `.env.local` and fill in the values from your Supabase
# project (Settings → API). When these are left blank the app stays in
# single-user, local-only mode (no login, all data lives in IndexedDB).

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
